import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.chakuy.ofiuco',
  appName: 'ofiuco',
  webDir: 'dist/y',
  server: {
    androidScheme: 'https'
  }
};

export default config;
